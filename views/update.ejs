<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Update | <%= title %></title>
</head>
<body>
    <main>
        <div class="updateBlog">
            <h3>Create a new blog here</h3>

            <form action="/blog/update/<%= blogresult.id %>" method="POST">
                <div>
                    <label for="title">Blog Title:</label>
                    <input type="text" name="title" id="title" value="<%= blogresult.title %>" placeholder="Enter your blog title..." required>
                </div>
                <div>
                    <label for="snippet">Blog Snippet:</label>
                    <input type="text" name="snippet" id="snippet" value="<%= blogresult.snippet %>" placeholder="Enter your blog snippet..." required>
                </div>
                <div>
                    <label for="body">Blog Body:</label>
                    <textarea name="body" id="body" cols="30" rows="10" placeholder="Enter your blog details..."
                        required><%= blogresult.body %></textarea>
                </div>
                <button type="submit">Update</button>
            </form>
        </div>
    </main>
    
</body>
</html>